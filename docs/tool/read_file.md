# `read_file`

按行读取文件。生产级安全措施：

- **Containment**：路径超出项目根目录拒绝
- **大小限制**：>10MB 拒绝
- **二进制检测**：40+ 扩展名黑名单
- **行对齐截断**：截断在最近换行符
- **元数据前缀**：`[文件] xxx.py | 243行 | 8KB | 行 1-243`

参数：
- `path` (必填)：文件路径（相对或绝对）
- `start_line` (可选)：起始行号 1-based
- `end_line` (可选)：结束行号 1-based（含）
